{
  "name": "@1delta-x/modules-permit-transfer",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "forge build --root ../.. ",
    "test": "forge test -vvv --root ../.. --match-path 'packages/modules-permit-transfer/**'",
    "snapshot": "forge snapshot --root ../.. --match-path 'packages/modules-permit-transfer/**'"
  }
}
